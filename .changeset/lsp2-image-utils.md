---
"@chillwhales/lsp2": minor
---

Remove `image-utils` from public API (moved to `@chillwhales/utils`). `findBestImage`, `findSmallestImage`, `findBiggestImage`, and `ImageSize` are no longer exported from this package.

{
  "name": "@chillwhales/lsps",
  "private": true,
  "description": "LUKSO Standards Packages — TypeScript implementations of LSP standards",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": ">=18"
  }
}
